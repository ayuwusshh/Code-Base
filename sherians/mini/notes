/*
1. setting up parsers for the project 
  app.use(express.json());
  app.use(express.urlencoded({ extended: true }));

2. setting up ejs
  install-> npm i ejs from npm
  setup ejs as view engine

3. setting up pulic static files
  app.use(express.static(path.join(__dirname,'public')));

4. Dynamic Routing
  open browser and type the url you need and press enter
  create thatr url and send res
  now add : before the dynamic part of the url
*/